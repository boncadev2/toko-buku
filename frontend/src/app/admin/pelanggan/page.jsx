"use client";
import {useEffect,useState} from "react";import {api} from "../../../lib/api";import AdminShell,{adminHeaders,ErrorBox,Loading} from "../../../components/AdminShell";
export default function Page(){const [customers,setCustomers]=useState(null),[error,setError]=useState(""),[search,setSearch]=useState("");const load=()=>api(`/admin/customers${search?`?search=${encodeURIComponent(search)}`:""}`,{headers:adminHeaders()}).then(r=>setCustomers(r.data||[])).catch(()=>setError("Data pelanggan tidak dapat dimuat."));useEffect(()=>{load();},[]);return <AdminShell title="Pelanggan" description="Pantau akun pelanggan yang terdaftar di toko ini."><form onSubmit={e=>{e.preventDefault();load();}} className="mt-6 flex gap-3 rounded-2xl bg-white p-4 shadow-sm"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Cari nama, email, atau nomor HP" className="min-w-0 flex-1 rounded-xl border px-4 py-3 text-sm"/><button className="rounded-xl bg-blue-700 px-5 py-3 text-sm font-bold text-white">Cari</button></form>{error&&<ErrorBox message={error}/>} {!customers?<Loading/>:<div className="mt-6 overflow-x-auto rounded-3xl bg-white p-5 shadow-sm"><table className="w-full min-w-[650px] text-left text-sm"><thead className="border-b text-xs uppercase text-slate-400"><tr><th className="pb-3">Nama</th><th>Email</th><th>Telepon</th><th>Status</th><th>Terdaftar</th></tr></thead><tbody>{customers.map(c=><tr key={c.id} className="border-b border-slate-100"><td className="py-4 font-bold">{c.name}</td><td>{c.email}</td><td>{c.phone||"—"}</td><td><span className="rounded-full bg-emerald-50 px-2 py-1 text-xs font-bold text-emerald-700">{c.status}</span></td><td>{new Date(c.created_at).toLocaleDateString("id-ID")}</td></tr>)}</tbody></table>{!customers.length&&<p className="py-10 text-center text-slate-400">Pelanggan tidak ditemukan.</p>}</div>}</AdminShell>}
