"use client";
/* eslint-disable @next/next/no-html-link-for-pages */
import { use, useState } from "react";
import { useRouter } from "next/navigation";
import { api } from "../../../lib/api";
export default function AuthPage({ params }) { const { mode } = use(params); const router=useRouter(); const login=mode==="login"; const [message,setMessage]=useState(""); const submit=async(e)=>{e.preventDefault();const f=new FormData(e.currentTarget);try{const data=await api(`/auth/${login?"login":"register"}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(login?{login:f.get("login"),password:f.get("password")}:{name:f.get("name"),email:f.get("email"),password:f.get("password"),password_confirmation:f.get("password")})});localStorage.setItem("token",data.data.token);router.push(data.data.user?.is_admin?"/admin":"/akun");}catch{setMessage("Data tidak valid atau layanan belum dapat dihubungi.");}};return <main className="grid min-h-screen place-items-center bg-blue-50 p-4"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-sm"><a href="/" className="text-sm font-bold text-blue-700">← Bukupagi</a><h1 className="mt-6 text-3xl font-black">{login?"Masuk":"Daftar"}</h1>{!login&&<input name="name" required placeholder="Nama" className="mt-6 w-full rounded-xl border p-3"/>}<input name="login" required placeholder={login?"Email atau nomor HP":"Email"} className="mt-3 w-full rounded-xl border p-3"/>{!login&&<input name="email" required placeholder="Email" className="mt-3 w-full rounded-xl border p-3"/>}<input name="password" type="password" required placeholder="Password" className="mt-3 w-full rounded-xl border p-3"/><button className="mt-5 w-full rounded-xl bg-blue-700 p-3 font-bold text-white">{login?"Masuk":"Buat akun"}</button>{message&&<p className="mt-4 text-sm text-rose-600">{message}</p>}</form></main>; }
